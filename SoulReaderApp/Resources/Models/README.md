# Models

Place your ML model files here:

- `*.mlmodelc` — compiled CoreML model bundles
- `*.onnx` — ONNX model files (used with `onnxruntime-objc`)

These files are intentionally excluded from version control via `.gitignore`.
Add them to the Xcode target membership so they are bundled into the app.
